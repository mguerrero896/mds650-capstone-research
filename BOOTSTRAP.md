Bootstrap commit created via REST API while git-transport issues are resolved.
The real history arrives by force-push over SSH; meanwhile the full-history bundle lives in the backup-20260817 release.
